import { Container } from "react-bootstrap";

export const Header = () => {
    return (
        <Container style={{ borderBottom: "1px black solid" }}>
            <h1>
                Header
            </h1>
        </Container>
    );
}